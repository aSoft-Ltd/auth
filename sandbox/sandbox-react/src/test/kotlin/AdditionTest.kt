import tz.co.asoft.locators.SandboxLocator
import kotlin.test.Test
import kotlin.test.assertEquals

class AdditionTest {
    @Test
    fun should_add_correctly() {
//        val locator = SandboxLocator(TODO(), TODO(), TODO())
        assertEquals(4, 2 + 2)
    }
}